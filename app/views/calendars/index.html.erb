<h1> My Calendars </h1>
<% @calendar_links.each do |calendar| %>
  <%= link_to calendar[:name], calendar_path(CGI::escape(calendar[:url])) %>
<% end %>

<h1>Calendars Atom feed</h1>
<pre><%= h(@calendars_atom) %></pre>
